{# TODO: test & fix #}
{% set pageDate = page.header.sitemap.lastmod ?? page.header.date ?? page.date %}

<span class="blog-date">
    <time class="dt-published" datetime="{{ pageDate|date("c") }}">
        <i class="fa fa-calendar"></i> {{ pageDate|date(system.pages.dateformat.short) }}
    </time>
</span>
